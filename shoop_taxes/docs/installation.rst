============
Installation
============

At the command line::

    $ easy_install dj-package

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-package
    $ pip install dj-package
